id: no-non-constructor-object-id
message: Use the constructor style
severity: warning
language: typescript
rule:
  all:
    - any:
      - pattern: Types.ObjectId($A)
      - pattern: Types.ObjectId()
    - not:
        regex: "^new "
