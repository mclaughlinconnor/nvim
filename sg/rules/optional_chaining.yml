id: optional_chaining
message: Just use optional chaining
severity: warning # error, warning, info, hint
language: TypeScript
rule:
  pattern: $PROP && $PROP()
fix:
  $PROP?.()
# utils: Extract repeated rule as local utility here.
# note: Add detailed explanation for the rule.
